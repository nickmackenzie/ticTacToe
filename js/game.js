/*----- constants -----*/
let playerBoard = document.querySelector("div#game-board");
let space = document.querySelectorAll("div.square");
/*----- app's state (variables) -----*/
let playerTurn = -1;
/*----- cached element references -----*/
/*----- event listeners -----*/

/*----- functions -----*/
function playerClick(e) {
  console.log("her");
}
